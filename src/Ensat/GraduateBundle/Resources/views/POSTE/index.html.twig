{% extends '::base.html.twig' %}

{% block body -%}
    <h1>POSTE list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Intutile</th>
                <th>Datedebut</th>
                <th>Datefin</th>
                <th>Entreprise</th>
                <th>Lieudetravail</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('poste_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.intutile }}</td>
                <td>{% if entity.datedebut %}{{ entity.datedebut|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.datefin %}{{ entity.datefin|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.entreprise }}</td>
                <td>{{ entity.lieudetravail }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('poste_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('poste_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('poste_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
