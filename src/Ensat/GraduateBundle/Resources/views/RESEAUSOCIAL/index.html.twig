{% extends '::base.html.twig' %}

{% block body -%}
    <h1>RESEAUSOCIAL list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Designation</th>
                <th>Identifiant</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('reseausocial_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.designation }}</td>
                <td>{{ entity.identifiant }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('reseausocial_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('reseausocial_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('reseausocial_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
