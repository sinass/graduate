{% extends '::base.html.twig' %}

{% block body -%}
    <h1>RESEAUSOCIAL</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Designation</th>
                <td>{{ entity.designation }}</td>
            </tr>
            <tr>
                <th>Identifiant</th>
                <td>{{ entity.identifiant }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('reseausocial') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('reseausocial_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
